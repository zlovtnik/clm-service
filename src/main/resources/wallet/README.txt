# Oracle Wallet Configuration
# Copy these files to the wallet location specified in application.yml

# Required files in wallet directory:
# - tnsnames.ora (TNS name definitions)
# - sqlnet.ora (network configuration)
# - cwallet.sso (auto-login wallet)
# - ewallet.p12 (optional, for manual login)

# Environment variables:
# ORACLE_WALLET_LOCATION=/opt/oracle/wallet
# ORACLE_TNS_NAME=clm_db
